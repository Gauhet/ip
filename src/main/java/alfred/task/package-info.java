/**
 * Holds the tasks and the list they are kept in. {@link alfred.task.Task} is
 * what every kind shares; {@link alfred.task.ToDo}, {@link alfred.task.Deadline},
 * and {@link alfred.task.Event} differ in the dates they carry.
 */
package alfred.task;
