/**
 * Holds one {@link alfred.command.Command} subclass for each thing the user
 * can ask for. A command that alters the list saves it; the ones that only
 * show tasks save nothing.
 */
package alfred.command;
