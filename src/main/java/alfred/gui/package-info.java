/**
 * Provides the graphical version of Alfred, built with JavaFX. The window asks
 * {@link alfred.AlfredTheButler} for an answer to every line the user sends,
 * so it runs the same commands as the console version and saves to the same
 * file.
 */
package alfred.gui;
