/**
 * The graphical version of Alfred, built with JavaFX.
 *
 * <p>{@link alfred.gui.Launcher} starts the application,
 * {@link alfred.gui.Main} builds the window, and
 * {@link alfred.gui.DialogBox} is one message within it.
 *
 * <p>Nothing here is connected to the chatbot yet. The window shows the layout
 * and a sample exchange, and the text field and the button do nothing when
 * used. The text version of the program in {@code alfred} is still where the
 * commands are read and answered.
 */
package alfred.gui;
